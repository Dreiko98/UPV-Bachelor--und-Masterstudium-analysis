# Ejemplo de archivo .env (no enviar a Git)
# Copia como .env y completa con tus valores

# Variables del Dashboard
VITE_API_URL=https://api.ejemplo.com
VITE_CSV_PATH=/panel_maestro_UPV_LIMPIO.csv

# Nota: Para producción en Netlify, no necesitas configurar estas variables
# El CSV se sirve automáticamente desde la carpeta public/
